const { Message, MessageEmbed, Client } = require('discord.js')

module.exports = {
    name: 'blackjack',
    /**
     * @param {Client} client
     * @param {Message} message
     * @param {String[]} args
     */
    run: async(messsage, client, args) => {
        
    }
}